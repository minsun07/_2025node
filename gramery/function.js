function 문자출력함수(문자){
    console.log('문자출력함수' + 문자);
}
문자출력함수('a');

// 기존 함수
문자출력익명함수 = function(문자){
    console.log('문자출력익명함수' + 문자);
};
문자출력익명함수('swag');

// 화살표 함수
문자출력익명함수 = (문자) =>{
    console.log('문자출력익명함수' + 문자);
};
문자출력익명함수('swag');